'use strict';

const { voteResolve } = require('express-handler-loader')('ufwd_survey_util');

const assert = require('assert');

describe('surveyResolve test', function() {
	describe('argument 0 is not an object test', function() {});

	describe('argument 1 is not an object test', function() {});

	describe('argument 1 is not an string test', function() {});

	describe('topic is not existed test', function() {});

	describe('create statistic test', function() {
		describe('the option is not recorded test', function() {
            describe('the option is not exited', function () {

            });
        });

		describe('the option is recorded test', function() {});
	});
});